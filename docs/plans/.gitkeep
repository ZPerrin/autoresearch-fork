Implementation plans (superpowers-flow scaffolding) land here; each is pruned once implemented
and its essence distilled (see docs/README.md). Kept so the docs/plans/ path
resolves while the directory is empty.
